node_modules/
.DS_Store
*.log
.wrangler/
.netlify/

# dist/ is intentionally committed: wrangler serves it as the static asset
# directory, so the deployed branch must carry the built site.

# JetBrains IDE settings
.idea/

# Working document, kept out of the repository on purpose.
WEBSITE_REVAMP_PLAN.md
